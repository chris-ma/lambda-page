import { PlaceholderPage } from "@/components/marketing/PlaceholderPage";

export default function PrivacyPage() {
  return (
    <PlaceholderPage
      eyebrow="Privacy"
      title="Privacy policy is next"
      body="The tracking snippet records selectors and event types only — never form field values or personal data. A full policy will land here before public launch."
    />
  );
}
