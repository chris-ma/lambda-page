import { PlaceholderPage } from "@/components/marketing/PlaceholderPage";

export default function DocsPage() {
  return (
    <PlaceholderPage
      eyebrow="Docs"
      title="Documentation is next"
      body="For now, the dashboard itself is the documentation — connect a page and every check explains its own fix inline."
    />
  );
}
